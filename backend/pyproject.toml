[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
# BLE001/S110: health/lifespan probes map any backend failure to a status string
ignore = ["BLE001", "S110"]
